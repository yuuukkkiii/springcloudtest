server:
  port: 8040
spring:
  application:
    name: order-service
  cloud:
    nacos:
      discovery:
        server-addr: 8.142.110.33
        username: nacos
        password: nacos
        namespace: public
#spring boot 默认的日志级别是info，feign的debug日志级别就不会输入
logging:
  level:
    order.feign: debug
feign:
  client:
    config:
      product-service:
        logger-level: BASIC